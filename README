Pre-requisite
==================================================
1. JDK 1.8+ installed
2. Maven 3 installed
3. Eclipse 4.7 Oxygen installed
4. Git installed and able to run mvn, java in bash

Workspace Setup
==================================================
1. git clone to local disk
   > git clone https://github.com/alfredyctan/filuname.git
2. start eclipse and setup the "Java->Installed JREs" and "Maven->Installations"
3. import the parent and child project by "Import -> Existing Maven Project"
4. on parent project, "Run As -> Maven install"

Run/Test the application
==================================================
1. On /filuname/src/main/java/org/alf/filuname/FilunameLauncher.java, then "Run As -> Java Application"
2. Copy /filunamesrc/test/resources/import/data.csv to /filuname/target/import, import will be trigger every 5 second
3. open brower with URL : localhost:10080/index.html
4. exclusion list will be updated every minute from the web target
 
Outstanding Backlog
==================================================
1. Create the assembly package base on the target deployment server
2. add requiredjs on HTML frontend to control dependency
3. add unit test on angular controller
4. use npm/bower for dependency management
5. use grunt to build the javascript package